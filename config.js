// Supabase project config. The anon key is public by design (row-level security
// protects the data). Filled in once the project exists; leave empty to run the
// app fully offline with localStorage only.
window.SB_CONFIG = { url: '', key: '' };
